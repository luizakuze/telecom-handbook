./gradlew tasks
./gradlew run -> compila e executa
./gradlew run -q --console=plain -> menos coisas ao executar
Empacotar app em Java -> JAR 
./gradlew jar -> pessoa executa comandos java p executar

laboratórios/lab-gradle/app/build/install/app
./bin/app -> p executar aplicação

gradlew run bota o deamon do gradlew e toda vez exeuta com o gradlew

jdk + gradlew p compilar
jvm p executar p dai [e gerado um bytecode


a pessoa que receber o arquivo java só precisa do jre 

---

dokcer multistage
imagem do gradle tem jdk ... mutiaaaaaa coisa (pesada)
 docker pull eclipse-temurin:21-jre-alpine